'use strict';

define(function () {
    angular.module("rcm.screens")
        .controller("screens.mailboxInfoController", function ($scope) {

        });

    return {
        title: "Mailbox Info"
    };
});